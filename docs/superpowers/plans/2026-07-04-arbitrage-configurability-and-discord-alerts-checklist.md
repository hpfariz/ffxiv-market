# Arbitrage Configurability and Discord Alerts Compliance Checklist

Source plan: `docs/superpowers/plans/2026-07-04-arbitrage-configurability-and-discord-alerts.md`

Status legend: pending, partial, done, deferred, blocked.

| Requirement | Implementation location | Verification | Status |
|---|---|---|---|
| Plan-compliance checklist exists and is updated before push/deploy | This file | Manual readback | done |
| Expanded arbitrage settings schema and API types | `migration/src/m20260704_000001_arbitrage_configurability.rs`; `ultros-db/src/entity/profile_arbitrage_settings.rs`; `ultros/src/web/api/profiles.rs`; `ultros-frontend/ultros-app/src/api.rs` | `cargo check -p ultros-db -p ultros`; hydrate check; `./check_ci.sh` | done |
| Separate table and alert configuration | `profile_arbitrage_settings` table/entity; `group_opportunities_for_surface` in `ultros/src/worker/arbitrage_daemon.rs`; settings UI in `market_dashboard.rs` | `rg -n "table_grouping_strategy|alert_grouping_strategy|group_opportunities_for_surface"` | done |
| Arbitrage endpoint/channel destination selection | `arbitrage_notification_destination` migration/entity; DB helpers in `ultros-db/src/profiles.rs`; API routes in `ultros/src/web.rs`; picker in `market_dashboard.rs` | `rg -n "arbitrage/destinations|arbitrage_notification_destination|selected_arbitrage_endpoint_ids"` | done |
| Expanded opportunity and digest snapshot fields | `arbitrage_opportunity` and `arbitrage_digest_state` entities; scanner SELECT/model construction; `digest_snapshot_hash` | `rg -n "selected_sell_reference_price|markdown_pct|execution_status|reference_avg_price"` | done |
| Item-level alert memory and profit-improvement resend rule | `arbitrage_item_alert_state` migration/entity; `alert_profit_improved`; `item_alert_states_from_candidates` | `rg -n "arbitrage_item_alert_state|alert_profit_improved|best_alerted_net_profit"` | done |
| Alert cadence/scheduling state and pending queue | `arbitrage_alert_schedule_state` and `arbitrage_pending_digest` tables/entities; interval/scanner-complete/scheduled due checks and pending enqueue/replay/cleanup in `ultros/src/worker/arbitrage_daemon.rs`; DB helpers in `ultros-db/src/profiles.rs` | `cargo check -p ultros-db -p ultros`; `rg -n "arbitrage_alert_schedule_state|cron_due|pending_rows_from_candidates|delete_arbitrage_pending_digest_item_keys"` | done |
| Per-endpoint delivery attempt tracking | `arbitrage_delivery_attempt` migration/entity; DB insert helper; `record_delivery_attempt` in worker | `rg -n "arbitrage_delivery_attempt|record_delivery_attempt"` | done |
| API endpoints for settings, preset, preview, test, reset, status | Expanded settings endpoint plus destinations/reset/apply-preset/preview/test/status routes in `ultros/src/web.rs`; handlers in `ultros/src/web/api/profiles.rs`; frontend client methods in `ultros-frontend/ultros-app/src/api.rs` | `cargo check -p ultros-db -p ultros`; hydrate check; `rg -n "settings/arbitrage/apply-preset|settings/arbitrage/preview|settings/arbitrage/test|arbitrage/alert-status"` | done |
| Migration backfill, constraints, and indexes | New migration adds columns/tables/indexes/backfill SQL and registers in migrator | `cargo check -p ultros-db`; `rg -n "idx_arbitrage|UPDATE profile_arbitrage_settings|create_supporting_indexes" migration/src/m20260704_000001_arbitrage_configurability.rs` | done |
| Preset defaults and custom transition | `CONSERVATIVE`, `BALANCED`, and `AGGRESSIVE` presets in `arbitrage_preset_active_model`; UI preset buttons call `apply_arbitrage_preset_api`; manual settings save remains `CUSTOM` | hydrate check; `rg -n "arbitrage_preset_active_model|Recommended Presets|apply_arbitrage_preset_api|CUSTOM"` | done |
| Destination/seller world validation and same-DC definition | API validates destination/seller scope and custom seller world ids; scanner resolves source/destination/reference sets and same-DC grouping; settings UI exposes custom seller world ids | `cargo check -p ultros-db -p ultros`; hydrate check; `rg -n "validate_seller_world_ids|resolve_arbitrage_world_sets|same_datacenter|Custom Seller World IDs"` | done |
| Reference pricing and sell price strategy rules | Scanner computes reference ask min/avg and applies `sell_price_strategy` plus markdown threshold | `rg -n "reference_prices|selected_sell_reference_price|sell_price_strategy|markdown_pct"` | done |
| Scanner new fields, grouping, filters, and performance safeguards | Scanner emits new fields, separate table/alert grouping, weekly velocity floor, markdown floor, and uses added DB indexes | `cargo check -p ultros-db -p ultros`; `rg -n "weekly_velocity_threshold|group_opportunities_for_surface"` | done |
| Alert frequency and immediate threshold behavior | Immediate threshold/rate limit, scanner-complete, interval, and scheduled cron modes implemented in worker; save-time cron validation added; empty digest behavior is configurable | `cargo check -p ultros-db -p ultros`; `rg -n "alert_frequency_mode|immediate_threshold_met|cron_due|validate_alert_schedule|alert_send_empty_digest"` | done |
| Settings UI sections, tooltips, validation, mobile layout | Settings UI exposes execution/pricing, table behavior, alert behavior, cadence/immediate, endpoint picker. Fields use visible `SettingHelpLabel` info icons | hydrate check; `rg -n "Execution & Pricing|Table Behavior|Alert & Digest Behavior|Cadence & Immediate Alerts"` | done |
| Digest history reset and alert preview/test-send UI | Reset API/UI implemented; preview and test-send endpoints and settings UI actions implemented; test-send records delivery attempts | hydrate check; `rg -n "reset_arbitrage_delivery_state|Preview Digest|Send Test|preview_arbitrage_digest|test_arbitrage_digest"` | done |
| Discord card delivery, chunking, and fallbacks | `DeliveryEmbed` multi-embed path implemented for Discord channel/DM/webhook with WebPush text fallback; arbitrage builds per-opportunity cards; delivery chunks embeds into Discord-safe batches of 10 across multiple messages | `cargo check -p ultros-db -p ultros`; `rg -n "DeliveryEmbed|build_arbitrage_digest_embeds|embeds.chunks\\(10\\)|deliver_embeds_to_endpoint"` | done |
| Observability/status/logging additions | Per-endpoint delivery attempts, worker logs, pending count, recent attempts, and dedicated arbitrage alert-status API/UI implemented | `cargo check -p ultros-db -p ultros`; hydrate check; `rg -n "insert_arbitrage_delivery_attempt|record_delivery_attempt|get_arbitrage_alert_status|get_recent_arbitrage_delivery_attempts"` | done |
| Table header native title tooltip fix included | `ultros-frontend/ultros-app/src/routes/market_dashboard.rs` | `rg -n "<th .*title=" ultros-frontend/ultros-app/src/routes/market_dashboard.rs` | done |
| Guest profile loading does not log expected unauthenticated state as an error | `ultros-frontend/ultros-app/src/routes/market_dashboard.rs` | VM strict E2E initially failed on deployed app due `ApiError(NotAuthenticated)` console.error; local code now handles that as guest state before logging other profile-load errors | done |
| `AGENTS.md` Plan Compliance Gate included | `AGENTS.md` | `rg -n "Plan Compliance Gate" AGENTS.md` | done |
| Required checks pass | Local workspace | `cargo check -p ultros-db -p ultros`; `cargo check -p ultros-app --target wasm32-unknown-unknown --no-default-features --features hydrate`; Git Bash `./check_ci.sh` | done |
| E2E harness for settings/routing UI changes | `scripts/run_e2e.sh` | Local attempt blocked because neither `.env` nor `DATABASE_URL` is set; VM deployed-app route suite passed desktop+mobile with `REUSE_SERVER=1`, `/market` routes, Snap Chromium, and allow-list only for the already-fixed deployed guest-auth console message | partial |
